<UserControl x:Class="AnimeStudio.GUIV2.Views.SceneHierarchyView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AnimeStudio.GUIV2"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
             xmlns:ui="http://schemas.modernwpf.com/2019"
      >

    <ui:SimpleStackPanel Background="#1E1E1E">
        <TreeView x:Name="sceneHierarchyView">
            <TreeView.Resources>
                <!-- Template for FileTreeItem -->
                <HierarchicalDataTemplate DataType="{x:Type local:FileTreeItem}" 
  ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>

                <!-- Template for AssetsFileTreeItem -->
                <HierarchicalDataTemplate DataType="{x:Type local:AssetsFileTreeItem}" 
  ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>

                <!-- Template for GameObjectTreeItem -->
                <HierarchicalDataTemplate DataType="{x:Type local:GameObjectTreeItem}" 
  ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasModel}" Value="True">
                                        <Setter Property="Background" Value="LightBlue"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </ui:SimpleStackPanel>
</UserControl>
